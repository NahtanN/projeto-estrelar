import { Button } from '@nextui-org/react';
import { MetamaskIcon } from '../..';

const ConnectButton = () => {
  return <Button icon={<MetamaskIcon />}>Connect Wallet</Button>;
};

export { ConnectButton };
